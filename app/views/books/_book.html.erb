<!--プロフィール-->
<h1>User info</h1>
<div class="profile-container">
  <!--ログイン中のユーザー名を表示させたい　※下記のUserは小文字のはず。変数を渡せない、、-->
  <h3 class="profile-name"><%= User.name %></h3>


<!--新規投稿-->
<h1>New book</h1>
<%= form_with model:book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>